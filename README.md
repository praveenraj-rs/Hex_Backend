# Web backend with Node.js for Hexfuse user management

This is a web backend for Hexfuse that is built with Node.js. It provides a REST API for managing users.

## Getting started

To get started, you will need to install Node.js and the following dependencies:
